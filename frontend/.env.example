# Local development only: use your local backend while running Vite.
# Production builds must set VITE_API_BASE_URL in the deployment environment.
# Example: https://backend-your-project.up.railway.app/api
VITE_API_BASE_URL=https://backend-your-project.up.railway.app/api
# Do not commit frontend/.env to source control; only keep this example file.

